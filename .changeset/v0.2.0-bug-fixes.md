---
"@marcfargas/pi-tramp": patch
---

Bug fixes and CI improvements from real-world testing.

**Bug fixes:**
- Fix timeout seconds-to-milliseconds conversion (pi sends seconds, transport expects ms)
- Fix SSH auto-detection for Windows pwsh login shells (6 cascading bugs)
- Fix `cwd` now optional — auto-detected from remote home directory
- Add Zod validation to `createTarget` and `handleAdd` (was using `as` type assertion)
- Guard against `undefined` cwd in path resolution and bash exec wrapper
- Fix CRLF handling — pi's `createEditTool` handles it transparently

**SSH auto-detection improvements:**
- Marker-based shell probe (`PITRAMP_PWSH_<uuid>`) instead of version parsing
- Reconnect with clean `-NoProfile -NonInteractive` flags after detecting pwsh
- Handle `$LASTEXITCODE` being `$null` until first external program runs

**CI:**
- Unit tests on ubuntu-latest + windows-latest × Node 20/22/24
- Integration tests: Docker×bash, Docker×pwsh, SSH×bash, SSH×pwsh on Linux
- Windows container integration tests (Server Core + OpenSSH + pwsh 7 + Git for Windows)
- Cross-platform test helper (`test/helpers/platform.ts`)
